fastapi==0.111.0
uvicorn[standard]==0.30.1
# Always use latest yt-dlp from GitHub to keep up with site changes (e.g., Facebook/YouTube)
# Using the tarball avoids needing git in the image
yt-dlp @ https://github.com/yt-dlp/yt-dlp/archive/refs/heads/master.tar.gz
aiofiles==23.2.1
# Enable HTTP/2
httpx[http2]==0.28.1
# Brotli decoding (some CDNs compress segments aggressively)
brotli==1.1.0
curl_cffi==0.7.1