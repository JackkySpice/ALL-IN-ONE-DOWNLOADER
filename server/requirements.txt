fastapi==0.111.0
uvicorn[standard]==0.30.1
# Install latest yt-dlp available at build time to avoid resolution failures
yt-dlp
aiofiles==23.2.1
# Enable HTTP/2
httpx[http2]==0.28.1
# Brotli decoding (some CDNs compress segments aggressively)
brotli==1.1.0
curl_cffi==0.7.1
pytest==8.3.2